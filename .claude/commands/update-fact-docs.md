Read `factscheck.csv` from the project root and update the fact-check scenarios table in `README.md`.

The table lives in the "Adding fact-check scenarios" section, between the line that starts with "Current scenarios:" and the end of that section. Replace the entire markdown table with a freshly generated one that reflects every row in the CSV (skipping the header). Keep the surrounding prose and the rest of the README unchanged.

Table format:
| Query | Expected result |
|-------|----------------|
| <query> | `<result>` |